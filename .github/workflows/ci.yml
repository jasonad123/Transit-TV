name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      
    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '20'
        
    - name: Setup pnpm
      uses: pnpm/action-setup@v4
      with:
        version: latest
        
    - name: Install dependencies
      run: pnpm install

    - name: Build SvelteKit app
      run: |
        cd svelte-app
        pnpm install --frozen-lockfile
        pnpm build

    - name: Check build output
      run: |
        if [ ! -d "svelte-app/build" ]; then
          echo "Build failed - svelte-app/build directory not created"
          exit 1
        fi
        echo "Build successful - svelte-app/build directory created"

  docker-test:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Build Docker image (test only)
      uses: docker/build-push-action@v6
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: false
        cache-from: type=gha
        cache-to: type=gha,mode=max
        
    - name: Test Docker image
      run: |
        docker build -t transit-tv-test .
        docker run --rm -d -p 8080:8080 --name test-container transit-tv-test
        sleep 10
        curl -f http://localhost:8080 || exit 1
        docker stop test-container