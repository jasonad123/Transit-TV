{
  "name": "transit-tv-server",
  "version": "1.0.0",
  "main": "server/app.js",
  "dependencies": {
    "compression": "^1.8.1",
    "cookie-parser": "~1.4.7",
    "ejs": "~3.1.10",
    "errorhandler": "~1.5.1",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1"
  },
  "scripts": {
    "start": "node --env-file=.env server/app.js",
    "build": "cd svelte-app && pnpm install && pnpm build",
    "clean": "rm -rf node_modules svelte-app/node_modules svelte-app/.svelte-kit"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "private": true,
  "devDependencies": {
    "@sveltejs/mcp": "^0.1.11"
  }
}
