# Railway Environment Variables Template
# Copy these variables to your Railway service settings

# Application settings
NODE_ENV=production
PORT=8080
USE_SVELTE=true

# API Keys
# Get your API key from https://transitapp.com/apis
TRANSIT_API_KEY=your_transit_api_key_here

# Security
# Generate a secure random string for production
SESSION_SECRET=generate_secure_random_string_here

# REQUEST_TIMEOUT: API request timeout in milliseconds
REQUEST_TIMEOUT=10000

# CORS Settings
# Not needed for Railway production deployments - CORS is disabled in production
# Railway serves both frontend and backend on the same origin, so no CORS required
# ALLOWED_ORIGINS is only used in development mode

# Logging
LOG_LEVEL=info

# Unattended Setup - Enable automatic setup for demo
# When set to "true", the configuration screen will be skipped
UNATTENDED_SETUP=true

# UNATTENDED_LOCATION: Latitude and longitude for demo location
# Format: "latitude,longitude" (e.g., "40.7240,-74.0002" for New York City)
# Default: Cleveland RTA (suggested by project inspiration)
UNATTENDED_LOCATION=41.4993,-81.6944

# UNATTENDED_TITLE: Display title
UNATTENDED_TITLE=Transit Display Demo

# UNATTENDED_TIME_FORMAT: Time display format
# Options: "HH:mm" (24-hour) or "hh:mm A" (12-hour with AM/PM)
UNATTENDED_TIME_FORMAT=hh:mm A
